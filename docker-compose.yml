version: '3'
services:
  tests:
    image: openjdk:12-oracle
    stop_signal: SIGKILL
    stdin_open: true
    tty: true
    working_dir: $PWD
    volumes:
      - $PWD:$PWD
      - /var/run/docker.sock:/var/run/docker.sock
    command: ./gradlew clean build
